import React from 'react';

import {
    Avatar,
} from '@mui/material';

import {
    HeaderContainer,
} from './styles';

const Header = () => {
    return (
        <HeaderContainer>

        </HeaderContainer>
    );
}

export default Header;
